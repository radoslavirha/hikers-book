apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-graphql-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-graphql
  template:
    metadata:
      labels:
        app: mongodb-graphql
    spec:
      containers:
        - name: mongodb-graphql
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-graphql-srv
spec:
  selector:
    app: mongodb-graphql
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
