{
  "name": "@hikers-book/tsed-swagger",
  "version": "0.5.0",
  "description": "Hiker's Book Ts.ED Swagger",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "format": "eslint --ext .ts --fix src",
    "lint": "eslint --ext .ts src",
    "postinstall": "pnpm build || true"
  },
  "dependencies": {
    "@tsed/openspec": "^7.36.8",
    "@tsed/swagger": "^7.36.8"
  },
  "devDependencies": {
    "@hikers-book/eslint-config": "workspace:*",
    "@types/node": "^20.5.7",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">= 18.0.0",
    "pnpm": ">= 8.7.0"
  },
  "os": [
    "darwin",
    "linux"
  ]
}
